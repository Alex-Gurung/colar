data_module:
  target: src.datasets.converted_sft.ConvertedSFTDataModule
  dataset_name: gsm  # or math, etc.
  dataset_dir: ~  # REQUIRED: set via CLI, e.g. dataset_dir=/path/to/data
  tiny_dataset: False
  epoch_scaling: 1
